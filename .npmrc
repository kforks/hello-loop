# Archived project. Supply-chain hardening (2026-08-11): deny install-time
# lifecycle scripts, so a future `npm install` here cannot execute dependency
# code with the installing user authority.
ignore-scripts=true
